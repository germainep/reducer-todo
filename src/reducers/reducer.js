const reducer = (state, action) => {
  switch (action.type) {

    case 'ADDTODO':
      return {...state, }
    case 'CLEARCOMPLETED':
      return {...state, action}
    default:
      return state

  }

}
